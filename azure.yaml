name: dlthubarrow
metadata:
  template: dlthubarrow@0.0.1
infra:
  provider: bicep
  path: infra
services:
  benchmark-runner:
    host: containerapp
    project: .
    language: docker
    docker:
      path: ./Dockerfile
      remoteBuild: true
